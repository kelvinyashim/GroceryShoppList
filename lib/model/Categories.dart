import 'dart:ui';

enum Categories{vegetables, fruit,meat,dairy,sweets,carbs,spices,convenience,hygiene,other}

class Category {
 const Category(this.text, this.color, );
  final String text;
  final Color color;
}
